[package]
name = "maestro-midds-schema"
version = "0.1.1"
edition = "2021"
description = "Allfeat MIDDS pallet GraphQL types (MusicalWork, Recording, Release) for Maestro indexer clients"
license = "MIT OR Apache-2.0"
repository = "https://github.com/allfeat/indexer"
keywords = ["graphql", "blockchain", "substrate", "allfeat", "midds"]
categories = ["api-bindings", "web-programming"]

[dependencies]
maestro-graphql-schema = { version = "0.1.1", path = "../graphql-schema" }
async-graphql = { version = "7.0", default-features = false, features = ["chrono"] }
chrono = { version = "0.4", features = ["serde"] }
serde = { version = "1.0", features = ["derive"] }
